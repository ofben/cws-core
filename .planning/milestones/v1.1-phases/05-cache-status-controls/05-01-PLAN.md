---
phase: 05-cache-status-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - includes/class-cws-core-api.php
  - includes/class-cws-core-cache.php
autonomous: true
requirements:
  - CACHE-01
  - CACHE-02
  - CACHE-04

must_haves:
  truths:
    - "After every API fetch attempt (success or failure), a timestamp is stored in the database"
    - "After every API fetch attempt, the HTTP response code is stored in the database"
    - "When cache is cleared, both status options are deleted so the UI shows 'no cache' state"
  artifacts:
    - path: "includes/class-cws-core-api.php"
      provides: "Records cws_core_last_fetch_time and cws_core_last_fetch_status options after every fetch attempt"
      contains: "update_option.*cws_core_last_fetch_time"
    - path: "includes/class-cws-core-cache.php"
      provides: "Deletes status options alongside transients on clear_all()"
      contains: "delete_option.*cws_core_last_fetch"
  key_links:
    - from: "includes/class-cws-core-api.php"
      to: "wp_options table"
      via: "update_option()"
      pattern: "update_option.*cws_core_last_fetch"
    - from: "includes/class-cws-core-cache.php"
      to: "wp_options table"
      via: "delete_option()"
      pattern: "delete_option.*cws_core_last_fetch"
---

<objective>
Record last-fetch metadata (timestamp + HTTP status code) to persistent options after every API request attempt, and clear that metadata when the cache is cleared.

Purpose: Phase 5 needs the admin UI to display cache age and last fetch result. This plan lays the data foundation — storing the facts the UI will read.
Output: Two new wp_options entries written on every API attempt and deleted on cache clear.
</objective>

<execution_context>
@/Users/benest/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benest/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cache-status-controls/05-CONTEXT.md

<interfaces>
<!-- Key code the executor will modify. Extracted from codebase. -->

From includes/class-cws-core-api.php — fetch_job_data() method (lines 113–183):
The method builds a URL, checks cache, then calls make_request(). After make_request():
- Line 143: $response_code = wp_remote_retrieve_response_code( $response );
- Line 146: if ( 200 !== $response_code ) { ... return false; }
- Lines 172–175: cache->set() call after successful parse
- Returns $data on success, false on error

Status must be recorded in TWO places:
1. After non-200 response (line ~149, before return false) — failure path
2. After successful cache->set() (line ~177, before final return $data) — success path
Note: WP_Error path (line 136) also needs recording with status 0 (connection failed).

From includes/class-cws-core-cache.php — clear_all() method (lines 138–175):
After the $wpdb->query() delete and wp_cache_flush_group() call, add delete_option() calls for both status keys. Returns $deleted count — do not change return value.

Option key names (per user decision in CONTEXT.md):
- cws_core_last_fetch_time  — stores time() integer
- cws_core_last_fetch_status — stores HTTP response code integer (0 = connection error)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record fetch metadata in CWS_Core_API::fetch_job_data()</name>
  <files>includes/class-cws-core-api.php</files>
  <action>
    In fetch_job_data(), add update_option() calls to persist last-fetch metadata. Three insertion points:

    1. WP_Error path (around line 139, after is_wp_error check): Record failure with status 0 (connection error cannot return an HTTP code). Add before `return false`:
       update_option( 'cws_core_last_fetch_time', time() );
       update_option( 'cws_core_last_fetch_status', 0 );

    2. Non-200 response path (around line 149, after the $response_code !== 200 check): Record failure with actual HTTP code. Add before `return false`:
       update_option( 'cws_core_last_fetch_time', time() );
       update_option( 'cws_core_last_fetch_status', intval( $response_code ) );

    3. Success path (around line 177, after the cache->set() call, before the log and return $data): Record success with HTTP 200. Add:
       update_option( 'cws_core_last_fetch_time', time() );
       update_option( 'cws_core_last_fetch_status', 200 );

    No new method needed — three inline update_option() pairs directly in fetch_job_data().
    Both options use autoload=yes (default) — small scalars, no performance concern.
    Do NOT record status when returning from cache (the early cache-hit return at line 128) — only record on actual API requests.
  </action>
  <verify>
    <automated>grep -n "cws_core_last_fetch_time\|cws_core_last_fetch_status" "/Users/benest/Local Sites/etch2job/app/public/wp-content/plugins/cws-core/includes/class-cws-core-api.php"</automated>
  </verify>
  <done>Three update_option pairs appear in fetch_job_data() — one for WP_Error, one for non-200, one for success. Cache-hit path has no update_option calls.</done>
</task>

<task type="auto">
  <name>Task 2: Clear fetch metadata in CWS_Core_Cache::clear_all()</name>
  <files>includes/class-cws-core-cache.php</files>
  <action>
    In clear_all(), after the existing wp_cache_flush_group() block and before the log line, add:

       delete_option( 'cws_core_last_fetch_time' );
       delete_option( 'cws_core_last_fetch_status' );

    This ensures that after a manual cache clear, the admin UI shows "No cache — will refresh on next page load" rather than stale metadata from a previous fetch cycle.
    The return value ($deleted) must remain unchanged — do not include these in the count.
  </action>
  <verify>
    <automated>grep -n "delete_option.*cws_core_last_fetch" "/Users/benest/Local Sites/etch2job/app/public/wp-content/plugins/cws-core/includes/class-cws-core-cache.php"</automated>
  </verify>
  <done>Two delete_option() calls appear in clear_all() after the transient delete block. Return value of clear_all() is unchanged.</done>
</task>

</tasks>

<verification>
1. Both option writes exist in fetch_job_data() at the correct locations (WP_Error, non-200, success paths).
2. Cache-hit early return does NOT have update_option calls.
3. Both delete_option calls exist in clear_all() — positioned after transient cleanup, before log line.
4. PHP syntax is valid: `php -l includes/class-cws-core-api.php` and `php -l includes/class-cws-core-cache.php` return no errors.
</verification>

<success_criteria>
- update_option( 'cws_core_last_fetch_time', time() ) called on every live API request (success and failure)
- update_option( 'cws_core_last_fetch_status', $code ) called with correct code (0, HTTP error code, or 200)
- delete_option() for both keys called in clear_all()
- No options written when serving from transient cache
</success_criteria>

<output>
After completion, create `.planning/phases/05-cache-status-controls/05-01-SUMMARY.md`
</output>
